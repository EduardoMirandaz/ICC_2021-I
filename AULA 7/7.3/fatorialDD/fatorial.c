#include<stdio.h>

int main(){

    int i, x, aux, fatorial=0;
    //fatorial(i) = i * (i-1)*(i-2)*(i-3)
scanf("%d", &i);
i = x;
            for(; 1 < i; x--){

               fatorial = i*(i-1);
               aux = fatorial;
               x = aux;

            }printf("%d", fatorial);


return 0;}
